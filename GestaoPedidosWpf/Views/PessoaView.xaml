<UserControl x:Class="GestaoPedidosWpf.Views.PessoaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GestaoPedidosWpf.Views"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button x:Name="btnAdicionar" Command="{Binding AdicionarPessoaCommand}" Content="Adicionar" HorizontalAlignment="Left" Margin="353,69,0,0" VerticalAlignment="Top"/>
        <DataGrid ItemsSource="{Binding Pessoas}" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="0,108,0,85"/>

    </Grid>
</UserControl>
